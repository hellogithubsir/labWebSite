# 实验室官网 M5 视觉与交互验收记录

## DOCX 全页视觉检查

使用 Office CLI 1.0.139 将两个最终副本分页输出为 HTML 快照，再通过浏览器逐页检查可见页面。检查项目：缺字/方框、文字截断、元素重叠、异常分页、表格溢出、图片丢失。

| 文档 | 实际页数 | 已检查页码 | 结果 |
|---|---:|---|---|
| 中文版语言统一副本 | 71 | 1–71（逐页） | PASS |
| English language-aligned copy | 69 | 1–69（逐页） | PASS |

两份文档均未发现上述可见缺陷；中文文档的内嵌照片在对应页面正常显示。源 DOCX 未被覆盖。

## HTML 第 7 个 data-target 按钮

第 7 个按钮均从 Projects 面板显示后点击，目标为 `tab-contact`，点击后活动页签与可见面板均为 Contact：

| HTML | 按钮文案 | 目标 | 结果 |
|---|---|---|---|
| 中文版语言统一副本 | 查看合作方式 | `tab-contact` | 活动页签 `tab-contact`，面板 `panel-contact` |
| English language-aligned copy | View collaboration options | `tab-contact` | 活动页签 `tab-contact`，面板 `panel-contact` |

## HTML 页签键盘操作

两份 HTML 均使用真实页签元素的键盘操作验证，且活动页签、焦点和可见面板一致：

| 按键 | 中文版 | English |
|---|---|---|
| `ArrowRight` | `tab-home` → `tab-research` / `panel-research` | `tab-home` → `tab-research` / `panel-research` |
| `ArrowLeft` | `tab-research` → `tab-home` / `panel-home` | `tab-research` → `tab-home` / `panel-home` |
| `End` | `tab-home` → `tab-contact` / `panel-contact` | `tab-home` → `tab-contact` / `panel-contact` |
| `Home` | `tab-contact` → `tab-home` / `panel-home` | `tab-contact` → `tab-home` / `panel-home` |

本轮仅处理 M5 指定的视觉与交互验收；未进行 M6 工作，也未重复已通过的 M1–M4 全量审计。
